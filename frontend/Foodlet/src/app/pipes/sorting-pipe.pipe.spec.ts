import { SortingPipe } from './sorting-pipe.pipe';

describe('SortingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortingPipe();
    expect(pipe).toBeTruthy();
  });
});
